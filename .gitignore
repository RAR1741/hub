node_modules
.next
.env*
!.env.example
supabase/.temp
.superpowers
*.tsbuildinfo
next-env.d.ts
/test-results
/playwright-report
/blob-report
/playwright/.cache


# Graphify (local-only, regenerate with `graphify update .`)
graphify-out/

# Isolated per-branch worktrees (scripts/new-worktree.sh, WorktreeCreate hook)
**/.claude/worktrees/

# Isolated per-branch worktrees (any `git worktree add`; see scripts/install-git-hooks.sh)
/.worktrees/

# design-sync (claude.ai/design) converter working dirs — durable inputs under
# .design-sync/ (config.json, hub.css, conventions.md, NOTES.md, previews/) ARE committed
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
